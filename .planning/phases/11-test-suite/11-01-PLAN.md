---
phase: 11-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_wizard.bats
  - tests/unit/test_config.bats
autonomous: true
requirements:
  - TEST-04
  - TEST-05

must_haves:
  truths:
    - "BATS tests verify wizard asks Open Notebook and DB-GPT questions and respects non-interactive env vars"
    - "BATS tests verify Compose profile string includes optional tool profiles for all selection combinations"
  artifacts:
    - path: "tests/unit/test_wizard.bats"
      provides: "Open Notebook and DB-GPT wizard test cases"
      contains: "WIZARD_OPEN_NOTEBOOK"
    - path: "tests/unit/test_config.bats"
      provides: "Compose profile generation tests for optional tools"
      contains: "opennotebook"
  key_links:
    - from: "tests/unit/test_wizard.bats"
      to: "lib/wizard.sh"
      via: "source in setup"
      pattern: "source.*wizard.sh"
    - from: "tests/unit/test_config.bats"
      to: "lib/config.sh"
      via: "source in setup"
      pattern: "source.*config.sh"
---

<objective>
Add BATS unit tests for v1.1 wizard extensions (Open Notebook + DB-GPT questions) and Compose profile generation with optional tools.

Purpose: Cover TEST-04 (wizard optional tool questions) and TEST-05 (Compose profile generation with optional tools) requirements.
Output: Extended test_wizard.bats and test_config.bats with new test groups.
</objective>

<execution_context>
@/Users/gbot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gbot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-test-suite/11-CONTEXT.md

@tests/unit/test_wizard.bats
@tests/unit/test_config.bats
@tests/helpers/setup.bash
@lib/wizard.sh
@lib/config.sh
</context>

<interfaces>
<!-- Key functions from lib/wizard.sh that tests must exercise -->

From lib/wizard.sh:
- `_wizard_ask_open_notebook()` — interactive, returns "0" or "1"
- `_wizard_ask_dbgpt()` — interactive, returns "0" or "1"
- `run_wizard` — non-interactive path reads `INSTALL_OPEN_NOTEBOOK` and `INSTALL_DBGPT` env vars, defaults to "0"
- Exports: `WIZARD_OPEN_NOTEBOOK`, `WIZARD_DBGPT`
- Validates via `_validate_choice` against "0" "1"
- RAM warning at 16GB threshold shown once before optional tool questions

From lib/config.sh:
- `_build_compose_profiles()` — returns comma-separated profile string
- When `WIZARD_OPEN_NOTEBOOK=1`: appends `,opennotebook`
- When `WIZARD_DBGPT=1`: appends `,dbgpt`
- When both: appends both `,opennotebook,dbgpt`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add wizard extension tests to test_wizard.bats (TEST-04)</name>
  <files>tests/unit/test_wizard.bats</files>
  <action>
Append a new test group "Group 8: Optional tools wizard extensions (v1.1)" to tests/unit/test_wizard.bats with the following tests:

1. "non-interactive defaults optional tools to off (0)":
   - Call `run_wizard` with NON_INTERACTIVE=1, no INSTALL_OPEN_NOTEBOOK or INSTALL_DBGPT set
   - Assert `WIZARD_OPEN_NOTEBOOK` = "0" and `WIZARD_DBGPT` = "0"

2. "non-interactive respects INSTALL_OPEN_NOTEBOOK=1":
   - Export `INSTALL_OPEN_NOTEBOOK=1`
   - Call `run_wizard`
   - Assert `WIZARD_OPEN_NOTEBOOK` = "1"

3. "non-interactive respects INSTALL_DBGPT=1":
   - Export `INSTALL_DBGPT=1`
   - Call `run_wizard`
   - Assert `WIZARD_DBGPT` = "1"

4. "non-interactive enables both optional tools":
   - Export both `INSTALL_OPEN_NOTEBOOK=1` and `INSTALL_DBGPT=1`
   - Call `run_wizard`
   - Assert both `WIZARD_OPEN_NOTEBOOK` = "1" and `WIZARD_DBGPT` = "1"

5. "non-interactive dies on invalid INSTALL_OPEN_NOTEBOOK":
   - Export `INSTALL_OPEN_NOTEBOOK=2`
   - `run run_wizard`
   - assert_failure, assert_output --partial "Invalid INSTALL_OPEN_NOTEBOOK"

6. "non-interactive dies on invalid INSTALL_DBGPT":
   - Export `INSTALL_DBGPT=bogus`
   - `run run_wizard`
   - assert_failure, assert_output --partial "Invalid INSTALL_DBGPT"

7. "WIZARD_OPEN_NOTEBOOK and WIZARD_DBGPT are exported after wizard":
   - Call `run_wizard` with both tools enabled
   - Verify in subshell: `bash -c 'echo "${WIZARD_OPEN_NOTEBOOK}:${WIZARD_DBGPT}"'` = "1:1"

8. "interactive run_wizard with optional tools shows optional tool questions" (NON_INTERACTIVE=0):
   - Use the same /bin/bash -c subshell pattern from existing "interactive run_wizard calls all 7 questions" test
   - Pipe 9 newlines (7 base questions + 2 optional tool questions, both default No)
   - assert_output --partial "Open Notebook: no"
   - assert_output --partial "DB-GPT:       no"

Follow existing test patterns: use `run_wizard` directly (not via `run`) for variable assertions, use `run run_wizard` for failure assertions. Use the same setup() from the file (NON_INTERACTIVE=1, DETECTED_RAM_GB=32).
  </action>
  <verify>
    <automated>cd "/Users/gbot/projects/agmind macos" && bats tests/unit/test_wizard.bats</automated>
  </verify>
  <done>8 new tests pass covering optional tools wizard behavior: defaults off, env var override, both tools, validation failure, export check, interactive display</done>
</task>

<task type="auto">
  <name>Task 2: Add Compose profile tests to test_config.bats (TEST-05)</name>
  <files>tests/unit/test_config.bats</files>
  <action>
Append a new test group "CONFIG-08: Compose profiles with optional tools (v1.1)" to tests/unit/test_config.bats with the following tests:

IMPORTANT: The existing setup() does NOT set WIZARD_OPEN_NOTEBOOK or WIZARD_DBGPT. Each new test must export these variables before calling `_build_compose_profiles`. Default both to "0" in tests that don't want them.

1. "CONFIG-08: build_compose_profiles omits opennotebook when not selected":
   - Export WIZARD_OPEN_NOTEBOOK="0", WIZARD_DBGPT="0"
   - `run _build_compose_profiles`
   - assert_success, refute_output --partial "opennotebook"

2. "CONFIG-08: build_compose_profiles includes opennotebook when selected":
   - Export WIZARD_OPEN_NOTEBOOK="1", WIZARD_DBGPT="0"
   - `run _build_compose_profiles`
   - assert_success, assert_output --partial "opennotebook"
   - refute_output --partial "dbgpt"

3. "CONFIG-08: build_compose_profiles includes dbgpt when selected":
   - Export WIZARD_OPEN_NOTEBOOK="0", WIZARD_DBGPT="1"
   - `run _build_compose_profiles`
   - assert_success, assert_output --partial "dbgpt"
   - refute_output --partial "opennotebook"

4. "CONFIG-08: build_compose_profiles includes both optional tools":
   - Export WIZARD_OPEN_NOTEBOOK="1", WIZARD_DBGPT="1"
   - `run _build_compose_profiles`
   - assert_success, assert_output --partial "opennotebook"
   - assert_output --partial "dbgpt"

5. "CONFIG-08: COMPOSE_PROFILES in .env includes opennotebook when selected":
   - Export WIZARD_OPEN_NOTEBOOK="1", WIZARD_DBGPT="0"
   - Call `_load_or_generate_secrets` then `_render_env_file`
   - `grep -q "opennotebook" "${AGMIND_DIR}/.env"`

6. "CONFIG-08: COMPOSE_PROFILES in .env includes both optional tools":
   - Export WIZARD_OPEN_NOTEBOOK="1", WIZARD_DBGPT="1"
   - Call `_load_or_generate_secrets` then `_render_env_file`
   - grep for both "opennotebook" and "dbgpt" in .env

Follow existing test patterns exactly. The existing setup() sources config.sh and sets WIZARD_* defaults.
  </action>
  <verify>
    <automated>cd "/Users/gbot/projects/agmind macos" && bats tests/unit/test_config.bats</automated>
  </verify>
  <done>6 new tests pass covering all Compose profile combinations: none selected, only Open Notebook, only DB-GPT, both selected, .env substitution with one, .env substitution with both</done>
</task>

</tasks>

<verification>
Run full unit test suite to ensure no regressions:
```bash
cd "/Users/gbot/projects/agmind macos" && bats tests/unit/test_wizard.bats tests/unit/test_config.bats
```
All existing tests still pass, all new tests pass.
</verification>

<success_criteria>
- 8 new tests in test_wizard.bats covering TEST-04 (wizard optional tool questions)
- 6 new tests in test_config.bats covering TEST-05 (Compose profile generation)
- All tests pass with `bats` runner
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-suite/11-01-SUMMARY.md`
</output>
